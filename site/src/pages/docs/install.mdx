---
layout: ../../layouts/DocsLayout.astro
title: Install & quickstart
eyebrow: Docs · Start here
lede: Install cueloop, open your first review in the terminal, and wire the plan gate into your agent. A practical five-minute start.
prev: { title: "Overview", href: "/docs/" }
next: { title: "The review session", href: "/docs/concepts/review-session/" }
---

import InstallTabs from "../../components/InstallTabs.tsx";

cueloop is a single command-line tool. The fastest path is the native
installer - one curl line, or Homebrew - which drops a self-contained binary on
your PATH and stays there regardless of your Node setup. Package managers work
too if you already live in one.

<InstallTabs client:load />

The curl script detects your OS and architecture, downloads the matching binary
from GitHub Releases, and installs it to `~/.local/bin` (or a writable system
directory). The binary bundles its own runtime, so it needs neither Node nor a
separate Bun. Homebrew installs the same binary and keeps it current with
`brew upgrade`.

<div class="callout callout--note">
  <div class="callout__label">Using npm, bun, or pnpm? Pin your Node version</div>A global npm-style
  install writes the cueloop launcher into the bin directory of the active Node version. Switch Node
  - an nvm default drift, a new .nvmrc - and that launcher drops off your PATH. Pin the Node version
  you install under, or use the curl or Homebrew path, which do not depend on Node at all.
</div>

## First run

Run `cueloop` with no arguments to open the inbox. The inbox lists every pending
review across all types - plans, working-tree diffs, and pull requests - so you
always land on the same place.

```bash
cueloop
```

From there, three verbs jump straight to the artifact you want:

```bash
cueloop plan          # open the latest pending plan
cueloop diff          # open the working-tree diff
cueloop review <pr>   # pull a pull request in and review it
```

Inside a review you read the artifact, annotate the exact lines that matter, and
send a verdict. The verdict is the structured result your agent waits on.

## Wire an agent

The point of cueloop is the gate between what an agent proposes and what it does.
For Claude Code or Codex, the plan gate opens a review and blocks on your
verdict before the agent starts to build. The agent stays paused until you
send a verdict, and it builds only when you approve.

See [Agent integration](/docs/agents/) for the exact hook setup.

That is the whole quickstart. cueloop reads an optional TOML config for
keybindings, the review surface, the theme, and Obsidian export, but every key
has a default - so you can start reviewing now and tune later.

<div class="callout callout--tip">
  <div class="callout__label">Start now, tune later</div>
  Every config key has a sensible default. See [Commands](/docs/reference/commands/) for the full
  command surface and [Configuration](/docs/reference/configuration/) for every setting.
</div>
