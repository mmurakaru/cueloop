---
"@cueloop/pi": minor
"@cueloop/adapters": minor
---

Package cueloop's shared skills with the pi extension, add `/cueloop:<workflow>` commands that invoke them, and remove the pi-only `/threads` command. Show repair guidance when cueloop and pi package versions differ.
