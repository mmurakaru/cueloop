---
"cueloop": patch
---

The primitive is now called a thread everywhere - the UI strings, the docs, and the concepts page - matching how it is used; the wire and CLI vocabulary is unchanged. The GitHub connect screens over SSH also read better: they show the short link and the code to type (GitHub no longer prefills it), align cleanly, wrap without clipping, and the data-use line no longer overstates what is kept.
