---
"cueloop": patch
---

The manage-access dialog matches the submit dialog: same width, the handle input centered with breathing room, and one primary "create" beside a plain "cancel". Enter adds a handle, so the separate add button and the empty-state line are gone.
