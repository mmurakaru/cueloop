---
"cueloop": patch
---

Allowed handles in the manage-access dialog render as inline chips that each reveal an × on hover to remove, like a file tab. The handle input sits inline at the end of the same row - centered in the dialog - so you type and press enter to add the next chip without leaving the keyboard. The prompt now reads "Add collaborators GitHub handles".
