---
---

Release-flow only: repair the corrupted changesets pre-state, skip the version-pr job on the publish push, automate the Homebrew tap formula bump, and tap before `brew install` in the docs. No shipped package behavior changes.
