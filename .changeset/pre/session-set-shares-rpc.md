---
"cueloop": patch
---

The daemon can persist a thread's full set of share links in one call, so multiple links survive a reload; the legacy single-share fields are dropped once the collection is authoritative.
