---
"cueloop": patch
---

Handles in the manage-access dialog are now a local draft: adding or removing them changes nothing until create publishes the link, so closing or cancelling discards the draft and no handles survive it. The dialog also adopts the submit dialog's height, tightens the gap between handle chips, and reads "Add GitHub handles of collaborators".
