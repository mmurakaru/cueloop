---
---

Reserve the scrollbar gutter on the root so the layout width stays identical whether or not a vertical scrollbar is present, fixing the horizontal shift/strip on browsers with non-overlay scrollbars.
