---
---

docs: render the cueloop logo at the top of the README via an `<img>` (GitHub strips inline SVG), and trim the README - drop the Grammar and Architecture sections and the by-hand hook wiring. No package changes.
